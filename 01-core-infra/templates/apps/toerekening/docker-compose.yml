version: "3.9"

services:
  toerekening:
    build:
      context: .
    ports:
      - "3002:3000"
    restart: unless-stopped
    networks:
      - traefik_net

networks:
  traefik_net:
    external: true
