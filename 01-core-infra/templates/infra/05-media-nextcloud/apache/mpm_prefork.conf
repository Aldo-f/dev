# Prefork MPM tuned for Pi 5 (8GB RAM) — Nextcloud
# Each worker ~40-60MB RSS with tuned PHP; limit workers to fit in ~2.5GB

StartServers             3
MinSpareServers          3
MaxSpareServers          6
MaxRequestWorkers       40
MaxConnectionsPerChild  5000
