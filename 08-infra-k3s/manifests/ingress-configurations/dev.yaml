apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: dev
  namespace: default
spec:
  entryPoints:
  - websecure
  routes:
  - match: Host(`dev.aldof.duckdns.org`)
    kind: Rule
    services:
    - name: urbanfix
      port: 3000
  tls:
    certResolver: duckdns-resolver
