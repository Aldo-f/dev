apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: stantonius-aldof
  namespace: default
spec:
  entryPoints:
  - websecure
  routes:
  - match: Host(`stantonius.aldof.duckdns.org`)
    kind: Rule
    services:
    - name: wordpress
      port: 4001
  tls:
    secretName: stantonius-tls-secret