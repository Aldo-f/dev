apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: nextcloud
  namespace: default
spec:
  entryPoints:
  - websecure
  routes:
  - match: Host(`cloud.aldof.duckdns.org`)
    kind: Rule
    services:
    - name: nextcloud
      port: 80
  tls:
    secretName: nextcloud-tls-secret